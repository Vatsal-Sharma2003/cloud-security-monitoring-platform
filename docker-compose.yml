version: '3.9'

services:

  security-api:
    build: .
    container_name: security-api
    ports:
      - "5000:5000"

    depends_on:
      - redis

  redis:
    image: redis:latest
    container_name: redis-server

    ports:
      - "6379:6379"